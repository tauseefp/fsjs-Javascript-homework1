// Slope is m = (y2-y1)/(x2-x1). Find the slope between point (2, 2) and point(6,10)

const x1=30;
const x2=40;
const y1=50;
const y2=65;

const slope=(y2-y1)/(x2-x1);

console.log("The slope between point (2,2) and (6,10):",slope);